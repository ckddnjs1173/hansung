import Link from "next/link";
import { createExit } from "@/app/exits/actions";
import { exitOptions,listExitSettlements } from "@/lib/exits";
export const dynamic="force-dynamic";const v=(x:unknown)=>String(x??"");const won=(x:unknown)=>`${Math.round(Number(x??0)).toLocaleString("ko-KR")}원`;
export default function Page(){const rows=listExitSettlements(),opts=exitOptions();return <div className="space-y-6"><header><p className="text-sm font-semibold text-teal-700">퇴사정산</p><h1 className="mt-1 text-3xl font-bold">퇴사·최종 정산</h1><p className="mt-2 text-sm text-slate-500">퇴사일, 마지막 급여, 잔여연차, 퇴직금과 인수인계 상태를 한 화면에서 관리합니다.</p></header><div className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">퇴사정산 생성</h2><form action={createExit} className="mt-4 grid gap-3 sm:grid-cols-2"><select name="worker_id" required className="h-10 rounded-xl border px-3 text-sm sm:col-span-2"><option value="">근로자 선택 *</option>{opts.map(o=><option key={v(o.id)} value={v(o.id)}>{v(o.name)} · {v(o.site_name)} {v(o.job_name)}</option>)}</select><label className="text-xs font-semibold text-slate-500">퇴사예정일<input name="planned_exit_date" type="date" className="mt-1 h-10 w-full rounded-xl border px-3"/></label><label className="text-xs font-semibold text-slate-500">퇴사일 *<input name="exit_date" type="date" required className="mt-1 h-10 w-full rounded-xl border px-3"/></label><label className="text-xs font-semibold text-slate-500">최종 근무일<input name="last_work_date" type="date" className="mt-1 h-10 w-full rounded-xl border px-3"/></label><input name="exit_reason" placeholder="퇴사사유" className="h-10 rounded-xl border px-3 text-sm self-end"/><input name="remaining_leave_days" type="number" step="0.5" placeholder="잔여연차 일수" className="h-10 rounded-xl border px-3 text-sm"/><input name="unused_leave_pay" type="number" placeholder="미사용 연차수당" className="h-10 rounded-xl border px-3 text-sm"/><input name="severance_pay" type="number" placeholder="퇴직금" className="h-10 rounded-xl border px-3 text-sm"/><input name="other_payment" type="number" placeholder="기타 지급" className="h-10 rounded-xl border px-3 text-sm"/><input name="other_deduction" type="number" placeholder="기타 공제" className="h-10 rounded-xl border px-3 text-sm"/><label className="text-xs font-semibold text-slate-500">개인정보 삭제 예정일<input name="privacy_delete_date" type="date" className="mt-1 h-10 w-full rounded-xl border px-3"/></label><input name="note" placeholder="정산 메모" className="h-10 rounded-xl border px-3 text-sm sm:col-span-2"/><button className="rounded-xl bg-teal-700 px-4 py-2.5 text-sm font-bold text-white sm:col-span-2">퇴사정산 생성</button></form></section><section className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">퇴사정산 목록</h2><div className="mt-4 space-y-3">{rows.length?rows.map(r=><Link href={`/exits/${v(r.id)}`} key={v(r.id)} className="flex items-center justify-between rounded-xl border p-4 hover:border-teal-300"><div><b>{v(r.worker_name)}</b><p className="mt-1 text-xs text-slate-500">{v(r.site_name)} {v(r.job_name)} · 퇴사 {v(r.exit_date)}</p></div><div className="text-right"><p className="font-bold">{won(r.final_settlement_amount)}</p><span className="text-xs text-teal-700">{v(r.status)}</span></div></Link>):<p className="rounded-xl border border-dashed p-6 text-center text-sm text-slate-400">등록된 퇴사정산이 없습니다.</p>}</div></section></div></div>}
